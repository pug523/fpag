# Public headers

* `base/`: Basic utilities and debug utilities
* `build/`: Provides build target and host information macros
* `logging/`: Logger utilities (`SyncLogger` and `AsyncLogger`)
* `mem/`: Memory related utilities
* `str/`: String related utilities
* `fpag.h`: All in one header
